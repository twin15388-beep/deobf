# План работ по реконструкции Ouroboros (1:1)

Легенда: ✅ сделано · 🔜 следующее · ⏳ потом

## Сделано

| Что | Артефакт |
|---|---|
| Пул: 6641 поле, 0 расхождений; индекс = F-слот | `data/pool.json`, `data/pool_index.json` |
| Машина: 1761 стейт, pc `cKb[73]`, entry 3720 | `ouroboros_main_pruned.txt` (1.9 МБ) |
| API-карта: 575 настроек в 21 таблице | `API_MAP.md`, `data/api_map.json` |
| **B** экипировка | `ouroboros_equip.lua` |
| **C** парирование (1.5 с подтверждено трейсом) | `ouroboros_parry.lua` |
| **D** фарм: лут, сундуки, души, схематики, квесты | `ouroboros_farm.lua`, `data/D_MAP.md` |
| **A** скиллы (автокаст через `asGameScript`) | `ouroboros_skills.lua` |
| Боевые твики и пороги убийства | `ouroboros_combat.lua` |
| Ядро: сервисы, `bno`/`bpz`/`cKb`, арбитр, `Signal` | `ouroboros_core.lua` |
| Движок перемещения: `bob`, `Reach` (bqd), `MoveTo`, `SetCollide`, `Alive` | `ouroboros_move.lua` |
| Конфиг: 77 ключей → сеттеры, экспорт/импорт JSON (лимиты 262 144 / 2048) | `ouroboros_config.lua`, `data/UI_MAP.md` |
| UI: окно/вкладки/группы/виджеты из артефакта | `ouroboros_ui.lua` (+ `tools/gen_ui_widgets.py`, `tools/gen_ui_lua.py`, `data/ui_tree.json`) |
| Единая сборка (ядро + 8 модулей + слоты cKb + псевдонимы) | `ouroboros_recon.lua` (+ `tools/build_recon.py`, `tools/check_bundle.py`) |
| Проверка синтаксиса настоящим Luau | `tools/check_lua.sh`, `tools/quick_check.py` |
| Дымовой прогон вне игры (заглушки Roblox + реальный Luau) | `tools/smoke.sh` (+`smoke_stub.lua`, `smoke_tail.lua`) |
| Публичный API `cKb[51]` = тот же объект, что UI-конфиг: сеттеры + `Track` | `tools/build_recon.py` |

## 🔜 Следующее

0. ✅ **Архитектура «тика» разобрана** — отдельного общего диспетчера фич нет:
   верхняя машина (`cKb[73]`, entry 3720) — это тело скрипта, а работу ведут
   воркеры контроллеров и фоновые циклы. Карта: `data/TICK_MAP.md`.
   - ✅ воркер-раннер `cKb[84]` (F3818, канон — инлайн `function(AP, AQ)`) и
     остановка `bmX` (F363) переписаны 1:1 (`ouroboros_farm.lua`);
   - ✅ `bnl = cKb[51]["BlockWork"]`: `in validate` (F4744), `step` (F288),
     `reset` (F6287), `stats`, `entries`, `blockEntry` — в `ouroboros_parry.lua`;
   - ✅ обёртка Heartbeat `F4910` (флаг `bn9`, доклад
     `cKb[100]("auto parry scheduler: …")`) + подключение из `M.StartLoops()`;
   - ✅ цикл сводок `cKb[75] = task.delay(0, F5182)` (1 с) — `M.StartSummaryLoop()`;
   - ✅ планировщик блоков `cKb[65] = F3871` + вся цепочка защиты:
     `cKb[14]` (подписка на модель) → `boh = F324` (вход по анимации) →
     `bmN = F2590` (окно: earliest/latest/due/protectUntil/window, пинг `bol`)
     → `cKb[58] = F4381` (годность входа) → `cKb[128] = F853` (дистанция)
     → `cKb[108]` (пресет по анимации) → `cKb[143]` (боевой вход) →
     `boA = F5058` («block/none/parry»), `bo_ = F2854` (фазы отпускания),
     `bpa = F1572` (просьба отпустить), `bmL = F1290` (снять вход);
     копии алиасов разобраны (см. `data/TICK_MAP.md` §5.1);
     ✅ в том числе `cKb[17] = F3460` (папка анимаций `Assets.Animations`,
     папки `*_Combat_Anims`, конфиг `Global.Combat_presets` → `Presets`);
     осталось: хвост `F853` (направление);
   - ✅ ownership-цикл `bm8` (F3841) — ESP-подсветка мобов: `bpp = {marks={}}`
     (`cKb[99]["viewer"]`), метка `cKb[78] = F4034` (Highlight
     "OuroborosOuwlandOwnership", AlwaysOnTop, 0.7/0), снятие `cKb[112]`,
     `bpp["clear"] = F1060`, цвет `bm8` (60,255,120) при `ReceiveAge == 0`,
     иначе `bo9` (255,70,70); цикл `task.delay(0, …)`: `while bnB() do
     <проход> task.wait(0.2) end` → модуль `ouroboros_esp.lua`;
   - 🔜 слоты, которые ещё не вычитаны (в сборке громкая заглушка
     `missing_slot` + `warn`): 24 (объект скилла), 42 (ввод боя), 48 (уровень),
     63 (контейнер регионов), 76 (код задачи), 106 (обновление скиллов),
     122/123 (модуль игрока).
1. **Закрыть «не дочитано»** — списки в конце каждого модуля. Приоритет:
   - ✅ `cKb[77]`: `owns` F5285, `retire` F3455, `valid` F3170, `claim` F4419,
     `remaining` F5168, `backoff` F5871, `held` F1166, `busy` F2775,
     `refused` F6399 — **сделано**;
   - ✅ `boo()` = F3280 (список мобов) и `bon()` = F2440 (анти-АФК) — **сделано**;
   - ✅ лут-шаг: `ClaimReadiness` F2668, `Claimable`, `WaitReady` (cKb[86]),
     `BeginAttempt` (bmO/bp3 = F5074), `Reach` (bqd) — **сделано**;
   - ✅ квест-шаг: машины 14066→14065 и 3512→3517 расписаны 1:1, хелперы
     `cKb[98]` (пул 363/1968 = счётчик), `cKb[142]` (пул 774 = имя/tostring),
     `cKb[94]` (пул 2280 = `SignalEvent:ToServer("RemoveQuest", имя)` + 0.5 c),
     `cKb[110]` (пул 615 = запись квеста), `cKb[137]` (пул 931 = `Quests.Holder`),
     `boW` (пул 1412 = кулдаун `max(0, QuestCD−30 ∨ …)`), `cKb[52]` (пул 429 =
     квест в слоте) — **сделано** (осталось: тела `bnq`/`bpV`, `bpQ`, `cKb[13]`,
     и вопрос «какое из двух тел `bp9` действует»);
   - ✅ `cKb[59]` — список регионов (`LocalPlayer.Humanoids.Regions`, пул 1319) — **сделано**;
   - ✅ движок перемещения: арбитр `bob` (tween/token), `Reach` (bqd), `MoveTo`
     (cKb[120]), `CancelMove` (cKb[87]), `SetCollide` (cKb[97], опакованный
     предикат разрешён), `Alive` (bpm = пул 796), `WaitReady` (cKb[86]) —
     **сделано**, `ouroboros_move.lua` (осталось: сам реестр `cKb[51]`);
2. **Проверка поведением** — прогнать оригинал с рекордером v4 (хуки промптов) на
   сценарии D и сверить с `ouroboros_farm.lua`: лут, сундук, душа, квест.
3. **UI** — самая большая оставшаяся часть: меню на внешней библиотеке
   ObsidianUltra (`Library.lua` + `addons/SaveManager.lua`,
   `https://raw.githubusercontent.com/joustingmatch/ObsidianUltra/main/`).
   Уже сделано: карта `data/UI_MAP.md` (структура окна, группы, вкладки),
   конфиг-слой `ouroboros_config.lua` (77 прямых ключей + динамические
   семейства, экспорт/импорт с лимитами и текстами уведомлений 1:1).
   Осталось: тексты/дефолты виджетов из UI-кода (`a1c`, рендер ~6843+) и,
   если нужен запускаемый файл без сети, — минимальный аналог API библиотеки.

## ⏳ Потом

4. ✅ **Сборка запускаемого файла** — `ouroboros_recon.lua` собирается
   `tools/build_recon.py` (ядро + move/farm/skills/combat/equip/parry/config/ui,
   таблица слотов `cKb`, псевдонимы артефакта, строки пула `cKb[136]`,
   `Boot()`/`Setters()`); статическая проверка ссылок — `tools/check_bundle.py`.
   Осталось: непрочитанные тела оформлены заглушками (`bnq`, `bpQ`, `bp9`, `bnn`,
   `cKb[3]`, `cKb[48]`, `cKb[76]`, `bnN/bnZ/bn_/bnc/bpd/bps`) и шаги, которых
   ещё нет (рыбалка, данжи, очереди, вебхуки, тренировки).
5. **Сверка с оригиналом по сценариям** — эталонные трейсы (№1–№4) как приёмочные
   тесты: пары Blocking 1.5 с, `Item_Equip 0/2`, `AddQuest`, `VisitRegion`.
6. **Разбор оставшихся подсистем**: рыбалка, данжи, очереди, вебхуки,
   уведомления, тренировки (всё это есть в артефакте и в `API_MAP.md`).
   Ownership/подсветка — ✅ (см. п.2, `ouroboros_esp.lua`); остался сам ESP-экран
   (`bna = { on = {}, entries = {}, screen, ... }`, `cKb[147]` — палитра).

## Известные ловушки

* Песочница сбрасывает рабочее дерево к `00a51f2` — восстанавливать
  `git fetch origin arena/01a0d367-deobf && git reset --hard FETCH_HEAD`.
* В артефакте **две параллельные копии** кода (сдвиг ~5.5 тыс. строк) с разными
  именами временных переменных и иногда разными значениями слотов — сверять
  по содержимому, а не по номеру слота (см. `ouroboros_core.lua`, шапка).
  Уточнение 26.09: тела обеих копий ссылаются на **одни и те же номера слотов**
  (сверено: `cKb[8] = function()` @1 192 023 и `bpn` @1 660 803 — набор
  `{13,52,54,86,91,94,98,110,136,137,142}` совпадает), а различаются только
  **карты слотов в заголовках сборок** (98 → пул 363 «отмена контроллера» в
  первой сборке против пула 1968 «счётчик» во второй; 137 → пул 931 против
  пула 4555 и т.д.). Каноническая карта — заголовок второй сборки (с ним код
  самосогласован); `data/pool_index.json` — раскладка пула ДО перестановки
  (`out/pool_perm.json`), при этом 363/1968/774/615/931/4555 в перестановке не
  участвуют.
* `Combat_Service` — ремоут самой игры, Ouroboros его не шлёт.
* Лут/сундуки/души — через `ProximityPrompt`, а не через сигналы.
